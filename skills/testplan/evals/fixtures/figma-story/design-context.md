# SIMULATED Figma design context — fixtures/figma-story

The eval environment has NO Figma MCP server. This file stands in for what Step A would
receive from `get_screenshot` + `get_design_context` on the frame linked from the Story.
Treat it as the mockup itself; do not attempt a real Figma call.

backend: remote (simulated)
fileKey: aBcD1234EfGh

## Frame 1205:3312 — "Notifications · Default"
- Panel anchored under the bell icon, list of notification rows, newest at the top.

## Frame 1205:3390 — "Notifications · Empty state"
- No list rows. A centred bell-with-slash illustration.
- Heading: "You're all caught up"
- Body: "New notifications will appear here."
- No "Mark all as read" action is rendered in this state.
