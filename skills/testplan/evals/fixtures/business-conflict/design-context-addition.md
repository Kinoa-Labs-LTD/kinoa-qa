# SIMULATED Figma design context — fixtures/business-conflict (ADDITION variant)

The eval environment has NO Figma MCP server. This file stands in for what Step A would
receive from `get_screenshot` + `get_design_context` on the frame linked from
story-addition.md. Treat it as the mockup itself; do not attempt a real Figma call.

backend: remote (simulated)
fileKey: Zx9QwErTy012

## Frame 880:1510 — "Export · In progress"
- The "Download CSV" action is replaced by a progress row.
- Spinner with the label "Preparing your export…".
- A secondary "Cancel export" action sits beside the label.
- No banner and no helper text are rendered in this state.
